# Extractable components

No existing reusable layout components are available to extract. The current application is a single component, so component extraction is intentionally skipped before the first design draft.

